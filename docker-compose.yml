services:
  tresorpost:
    image: ghcr.io/dol-ch/tresorpost:0.1.6
    env_file: .env
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      # SQLite file appears on the host as ./data/tresorpost.db
      - ./data:/data
    restart: unless-stopped
